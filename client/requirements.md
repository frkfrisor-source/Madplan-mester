## Packages
framer-motion | Complex animations for the "generating" state and page transitions
date-fns | Handling dates and Danish localization
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
- The app uses a Scandinavian minimal aesthetic (off-whites, deep greens, crisp typography).
- OpenAI generation might take time; implementing polling for meal plan status is required.
- All UI text must be in Danish.
